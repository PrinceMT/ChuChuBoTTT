 exports.commands = {
     cr: function(arg, by, room) {
     If (!bot.canUser('cr' by, room) return false;
     Bot.say(by, room, ' rom.id = ' + room + ' | server.id = ' +  config.serverid + ' | server.port = ' + config.port 1 ' | server.ip = ' + config.server + '');
},
};
